let n = 2, z, i = 0.1, k = 3, theta = 1, e = 1;

z = Math.abs(-4);
let c = Math.sqrt(z);

let first = (Math.pow(Math.abs(-4)), 1 / n);
let second = (Math.exp((i) * ((theta + (2 * k * Math.PI)) / n)));
let answer = first * second;

console.log(answer);
